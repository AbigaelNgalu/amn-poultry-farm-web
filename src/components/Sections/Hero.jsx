import React from "react";
import { Button } from "../ui/Button";

const HeroSection = () => {
  return (
    <section
      id="home"
      className="relative bg-[url('/assets/images/farm-bg.jpg')] bg-cover bg-center text-white py-24 md:py-32 lg:py-40"
    >
      <div className="absolute inset-0 bg-black/50"></div>
      <div className="relative z-10 container mx-auto text-center px-4 md:px-8">
        <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6">
          Fresh & Healthy Poultry Products
        </h1>
        <p className="max-w-2xl mx-auto mb-8 text-base md:text-lg opacity-90">
         var _s = $RefreshSig$();
     import React, { useState } from "react";
     import Card from "../common/Card";
                    ^
      import { breedsData } from "../../data/breedsData";
      const ProductShowcase = () `{'}'}`
         From our farm to your table — organic eggs, fresh chicken, and premium feeds.
        </p>
        <div className="flex flex-col md:flex-row justify-center gap-4">
          <Button variant="primary">Shop Now</Button>
          <Button variant="outline">Learn More</Button>
        </div>
      </div>
    </section>
  );
};

export default HeroSection;
